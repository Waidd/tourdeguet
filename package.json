{
  "name": "tourdeguet",
  "description": "Rss feeds aggregator.",
  "version": "0.1.0",
  "author": "Thomas Cholley <cholley.t@gmail.com>",
  "main": "src/app.js",
  "scripts": {
    "configure": "cp feeds.json.example feeds.json && $EDITOR feeds.json",
    "start": "./bin/tourdeguet.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "feedparser": "^2.1.0",
    "request": "^2.79.0",
    "ws": "^2.0.3"
  }
}
